# GodsEye - Python dependencies
# Install with:
#   pip install -r requirements.txt

# ================================
# Core configuration and validation
# ================================
pydantic>=2.8.0,<3.0.0
PyYAML>=6.0.1,<7.0.0
python-dotenv>=1.0.1,<2.0.0

# ================================
# API server (currently used)
# ================================
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# ================================
# Database (PostgreSQL + ORM)
# ================================
SQLAlchemy>=2.0.30,<3.0.0
psycopg2-binary>=2.9.9,<3.0.0

# ================================
# Messaging / IoT transport
# ================================
paho-mqtt>=1.6.1,<2.0.0

# ================================
# HTTP client (test harness and API checks)
# ================================
requests>=2.32.0,<3.0.0

# ================================
# Logging and observability helpers
# ================================
python-json-logger>=2.0.7,<3.0.0

# ================================
# Testing and data generation
# ================================
pytest>=8.2.0,<9.0.0
Faker>=25.0.0,<30.0.0

# ================================
# Optional ML stack (roadmap / anomaly modeling)
# Keep installed for local experimentation.
# ================================
numpy>=1.26.0,<3.0.0
pandas>=2.2.0,<3.0.0
scikit-learn>=1.5.0,<2.0.0
