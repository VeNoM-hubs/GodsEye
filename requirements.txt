# Core Backend (Essential for Phase 1)
pydantic>=2.5.0
pyyaml>=6.0
python-dotenv>=1.0.0

# MQTT Communication (Phase 2)
paho-mqtt>=1.6.1

# Monitoring & Logging
python-json-logger>=2.0.0

# Testing
pytest>=7.4.0
faker>=20.0.0

# Note: ML packages (pandas, numpy, scikit-learn) will be added in Phase 8
# Note: API packages (fastapi, uvicorn) will be added in Phase 10
